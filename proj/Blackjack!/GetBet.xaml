<Window x:Class="Blackjack.GetBet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bet"
        Width="364"
        Height="120"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow" Closing="onclose" GotFocus="givefocus" KeyDown="foc">
    <Grid Height="73" VerticalAlignment="Top">
        <Label Height="28"
               Margin="5,9,213,0"
               VerticalAlignment="Top"
               VerticalContentAlignment="Center"
               Content="Please enter your bet:" />
        <TextBox x:Name="txtBet"
                 Height="23"
                 Margin="145,12,10,0"
                 VerticalAlignment="Top"
                 VerticalContentAlignment="Center"
                 TextWrapping="Wrap" TextChanged="txtBet_TextChanged" />
        <Button
                Margin="10,42,10,0"
                VerticalAlignment="Top"
                Content="OK" IsDefault="True" Click="Button_Click_1" />
    </Grid>
</Window>
